# layer-based-weighted_image_generation

🇷🇴
Script care genereaza diferite imagini folosind layere PNG si greutati pentru raritatea fiecarui element. Fiecare imagine unica si contine metadata.

Cerinte:
pip install pillow

🇺🇸
Script that generates different images using PNG layers and weights for the rarity of each element. Each image is unique and contains metadata.

Requirements:
pip install pillow
